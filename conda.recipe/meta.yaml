{% set build_number = "0" %}
{% set build_string = "py{}h{}_{}_{}".format(CONDA_PY, PKG_HASH, build_number, GLM_ARCHITECTURE) %}

package:
  name: quantcore.matrix
  version: {{ environ.get('GIT_DESCRIBE_TAG', '').lstrip('v') }}{% if environ.get('GIT_DESCRIBE_NUMBER', 0)|int != 0 %}.post{{ GIT_DESCRIBE_NUMBER }}+{{ GIT_DESCRIBE_HASH }}{% endif %}

source:
  git_url: ../


build:
  number: {{ build_number }}
  string: "{{ build_string }}"
  track_features:
    {{ "- quantcore-matrix-{}".format(GLM_ARCHITECTURE) if GLM_ARCHITECTURE != "default" else "" }}


requirements:
  build:
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}
    - llvm-openmp  # [osx]
  host:
    - python
    - cython
    - jemalloc     # [not win]
    - llvm-openmp  # [osx]
    - mako
    - numpy
    - pip
    - scikit-learn >=0.23
    - setuptools_scm
    - xsimd
  run:
    - python
    - joblib
    - numexpr
    - {{ pin_compatible('numpy') }}
    - pandas
    - scikit-learn >=0.23
    - scipy
    - sparse_dot_mkl >=0.4.0  # [not (osx and arm64)]

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - quantcore.matrix

about:
  home: https://github.com/Quantco/quantcore.matrix
  license: Proprietary
